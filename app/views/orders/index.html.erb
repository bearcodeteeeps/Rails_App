<p id="notice"><%= notice %></p>

<h1>Products</h1>

<table>
  <thead>
    
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        
        <td><%= link_to(image_tag("https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif"), order.paypal_url(order))%></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path %>